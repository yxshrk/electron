# Replicas — claim 3600 hackathon credits first:
#   1. one org admin creates an account at tryreplicas.com
#   2. claim at https://tryreplicas.com/dashboard/insforge-hackathon  (code: ainexus)
#   3. create an API key under Settings → API Keys
REPLICAS_API_KEY=sk_replicas_...

# Optional override (defaults to https://api.tryreplicas.com)
# REPLICAS_BASE_URL=
